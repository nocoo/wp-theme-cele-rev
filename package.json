{
  "name": "celerev-theme",
  "version": "1.0.0",
  "description": "CeleRev WordPress Theme - Modern Build System",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "compare": "bun run scripts/compare.ts",
    "setup": "bun run scripts/install-hooks.sh"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "csso": "^5.0.5"
  }
}
